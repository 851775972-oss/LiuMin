name: 哔哩哔哩UP主评论监控

on:
  workflow_dispatch:

jobs:
  monitor:
    runs-on: ubuntu-latest
    
    steps:
      - name: 测试Bark推送
        env:
          BARK_KEY: ${{ secrets.BARK_KEY }}
        run: |
          echo "BARK_KEY is: $BARK_KEY"
          curl "https://api.day.app/$BARK_KEY/测试/推送成功"
